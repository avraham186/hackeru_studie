import socket
s= socket.socket()- מייבאים ספרייה לקוד שלנו
s.connect(("1.1.1.1", 80))
s.send("hi")-  אחרי התחברות לשרת שולחים אליו א ת התווים
data = s.recv(2048)- מחכים לקבלת מידע מהשרת וקובעים שהמקסימום יהיה 2048
print datas.close()